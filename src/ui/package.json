{
  "name": "ui",
  "version": "1.0.0",
  "description": "UI for bee-travels",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm install && npm run build && cd ..",
    "dev": "./node_modules/.bin/concurrently --kill-others-on-fail 'npm run build' 'npm run client'",
    "start": "npm run build && node index.js",
    "client": "cd client && npm install && npm start"
  },
  "keywords": [
    "react",
    "node",
    "bee-travels"
  ],
  "author": "moficodes@gmail.com, magicmax33@gmail.com",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "query-string": "^6.9.0",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "concurrently": "^5.0.0",
    "nodemon": "^1.19.4"
  }
}
