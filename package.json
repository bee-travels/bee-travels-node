{
  "name": "bee-travels",
  "private": true,
  "workspaces": [
    "services/*"
  ],
  "scripts": {
    "start": "yarn workspaces foreach -pi -j 10 \"$@\" run start"
  },
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
